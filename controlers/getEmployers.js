// /* eslint-disable no-undef */
const getEmployersRouter = require('express').Router();
// const Service = require('../models/service');
// const user = require('../models/user');

// getEmployersRouter.get('/', async (request, response) => {
//   //Necesito  encontra el usuario que esta guardado en los servicios
// });

// // getEmployersRouter.patch('/:id', async (request, response) => {
// //   const { service } = request.body;
// //   await user.findByIdAndUpdate(request.params.id, { service });

// //   return response.sendStatus(200);

// // });



module.exports = getEmployersRouter;